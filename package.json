{
  "name": "cos_git_inspect",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Git status and diff tools over MCP stdio.",
  "type": "module",
  "bin": {
    "cos-git-inspect": "dist/server.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && vitest run --config vitest.config.ts",
    "check": "npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.6.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
